Simple tracking for VMM3a/SRS
=============================

How to run the synchronisation check (example):

```bash
python3 checkSync.py -f "/path/to/file/run75.root" -d "2 1 3 4"
```

The first detector in the list, is the time reference.